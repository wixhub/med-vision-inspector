version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: med_vision_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    restart: always